import { ComingSoon } from '@/components/coming-soon';

export default function OffersPage() {
  return (
    <ComingSoon
      title="Offer Management"
      phase="Phase 3"
      description="Bulk offer operations, expiry alerts, and backfill job configuration — replacing scattered Vast CLI commands with a single surface."
    />
  );
}
